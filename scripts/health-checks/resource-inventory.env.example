# Copy to resource-inventory.env and replace placeholders with real values.
# This file is sourced by read-only scripts only.

PRIMARY_REGION=ap-south-1
DR_REGION=ap-south-2

GLOBAL_DB_ID=REPLACE
DDB_TABLE=REPLACE
GLOBAL_REDIS_ID=REPLACE
MSK_REPLICATOR_ARN=REPLACE
DR_EKS_CLUSTER=REPLACE

SECONDARY_FQDN=
SECONDARY_HEALTH_CHECK_ID=REPLACE
